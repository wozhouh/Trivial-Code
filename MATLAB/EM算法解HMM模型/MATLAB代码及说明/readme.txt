% ############### 程序运行方法 ###############

% 直接运行文件夹的 Gaussian_EM_main.m 即可



% ############### 几点说明 ###############

% EM算法主要写在 gaussian_EM.m 里，其它m文件函数为辅助

% 初始参数设置：初始点根据样本点随机选择，最小迭代次数100，最大迭代次数1000，似然函数增长阈值10^(-10)

% 样本点分布图中不同的颜色表示该点产生自不同的高斯分布



% ############### 结果分析 ###############

% 由EM结果可以看出，不同的初始值选择最后基本都会达到收敛，但收敛结果并不相同，说明EM算法只能收敛到局部最优点，受初始值的选择影响较大。